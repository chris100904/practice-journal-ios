import SwiftUI

struct HomeView: View {
    var body: some View {
        VStack {
            Text("Welcome back,")
            Text("Christopher")
                .font(.title)
            Text("Weekly Digest")
        }
        .padding()
    }
}

